import { TextIcon } from '@/components/icons'
import { IconProps } from '@chakra-ui/react'
import React from 'react'

export const TextInputIconCopy = (props: IconProps) => (
  <TextIcon color="red" {...props} />
)
